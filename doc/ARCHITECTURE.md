## TASk_H

- it holds the blue print of task 
- created enums for STATUS and PRIORITY so there are predictable inputs from usr
- the class has a constructor which initialises everything to NULL like values
- there is a parameterised constructor which is for assigning values
- there are also getters and setter just in case we have to work with single function